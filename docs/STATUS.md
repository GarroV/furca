# Статус работ

> Снимок состояния на 17.09.2026, ведётся вручную. Обновляется при каждой остановке работы.
> Первая версия влита в `main` (ветки `feat/forge-v1` больше нет). Дизайн и план — в [docs/specs/](specs/) и [docs/plans/](plans/).
> Задачи живут в [Issues](https://github.com/GarroV/furca/issues) — их видно из любой сессии и с телефона.
>
> **Как этот документ ведётся.** Запись о свежем изменении добавляется вместе с датой в шапке: `test/docs.test.sh` сверяет дату снимка с самым поздним событием в тексте и краснеет, если шапка отстала. Поднять дату, не перечитав «Где мы», нельзя — снимок тем самым объявляется актуальным целиком. Правило заведено после того, как документ простоял с датой 31.08.2026 до 17.09.2026: любая честная запись валила прогон, поэтому записи просто не появлялись ([#116](https://github.com/GarroV/furca/issues/116)).

## Где мы

Идёт реализация первой версии по плану [docs/plans/2026-07-29-forge-v1.md](plans/2026-07-29-forge-v1.md) — 11 задач. Закрыто 7 из 11. Все команды системы написаны и обкатаны на служебном проекте — от одной фразы до работающего продукта, без владельца. Канал уведомлений развёрнут и проверен вручную. Живые прогоны идут с 07.08.2026; разбор их находок — основная работа с 19.08.2026, номера задач дошли до [#116](https://github.com/GarroV/furca/issues/116), открытых 38. Осталось то, что нельзя проверить без владельца: анкета на живом человеке и раскатка на настоящую площадку. Всё, что сделано, покрыто тестами; каждая задача прошла ревью с фактической перепроверкой (ревьюер сам воспроизводит негативные сценарии, а не верит отчёту исполнителя).

**Система переименована: Forge → FURCA** (09–10.09.2026, [#98](https://github.com/GarroV/furca/issues/98)). Команды теперь `ordo` (старт проекта), `fabrica` (стройка), `cursus` (сводка), `missio` (раскатка), `admissio` (каталог проверок); роли — `artifex` (блок-агент), `optio` (исполнитель), `norma` (сверка экрана), `exploratio` (исследование). Переехали и данные: `FURCA_HOME`, `FURCA_SECRET`, профиль в `~/.claude/furca/`, данные продукта в `docs/furca/`, репозиторий `GarroV/furca`. Хвосты, которые переименование задело по несущей части, дочищены семью коммитами 10.09; остаток ребрендинга — [#96](https://github.com/GarroV/furca/issues/96). Старые имена в этом документе ниже по тексту оставлены там, где они называют исторический факт, а не текущий механизм.

| Задача | Состояние | Issue | Коммиты |
|---|---|---|---|
| 1. Каркас: дефолты, профиль пользователя, установщик | ✅ закрыта | — | `54cfa45`, `f8a1433`, `a30964f` |
| 2. Форматы файлов состояния + фикстуры | ✅ закрыта | — | `fcec365`, `fdeb358` |
| 4. Шаблоны документации проекта | ✅ закрыта | — | `20ba858` |
| 6. Сводка о ходе стройки | ✅ закрыта | — | `aa3c812`, `7d04aee`, `f6d1758` |
| 3. Стартовая анкета | ✅ закрыта (решение делегировано) | [#1](https://github.com/GarroV/furca/issues/1) | `0b6f0e5`, `70c28d7` |
| 5. Протокол уведомлений в Telegram | 🔧 канал развёрнут, протокол переключён на него; **ждёт первой стройки** | [#2](https://github.com/GarroV/furca/issues/2) | `71d6988`, `0157dcc` |
| 7. Старт проекта: анкета → research → брейншторм → документация | 🔧 обкатан на служебном проекте; **на живом человеке анкета не проверена** | [#3](https://github.com/GarroV/furca/issues/3) | `31d6b35` + правки обкатки |
| 8. Автономная стройка (диспетчер) | ✅ закрыта (обкатана на служебном проекте) | [#4](https://github.com/GarroV/furca/issues/4) | `744b0d7` + 9 правок обкатки |
| 9. Раскатка на целевую площадку | 🚦 скилл написан, **смоук требует целевой площадки** | [#5](https://github.com/GarroV/furca/issues/5) | `70f64cf` |
| 10. README, ретро-петля | ✅ закрыта | [#6](https://github.com/GarroV/furca/issues/6) | `8403a38` |
| 11. Пилот на реальном мини-проекте | 🔧 первый живой прогон идёт с 07.08.2026; по его ходу заведены [#21](https://github.com/GarroV/furca/issues/21)–[#31](https://github.com/GarroV/furca/issues/31) | [#7](https://github.com/GarroV/furca/issues/7) | — |

Вне скоупа первой версии, но записано, чтобы не потерялось: [#8](https://github.com/GarroV/furca/issues/8)–[#10](https://github.com/GarroV/furca/issues/10) (упаковка в плагин, работа двоих над одним проектом, перенос стройки на домашний сервер). **Английский канон ([#11](https://github.com/GarroV/furca/issues/11)) из этого списка выведен 13.08.2026** — после решения делать систему передаваемой это блокер, а не «когда-нибудь».

## Что уже работает

- **Установка:** `./install.sh` — идемпотентно связывает скиллы системы с личным каталогом Claude Code и создаёт личный профиль из шаблона, не затирая существующий.
- **Настройки:** дефолты системы в `config.defaults.md`, личные переопределения — в `~/.claude/furca/profile.md` (в репозиторий не попадает). Тестовая площадка, канал уведомлений, лимит сверок со спекой, рекомендация модели для сессии.
- **Роли агентов стройки** (`agents/*.md`): блок-агент (`opus`), исполнитель внутри блока (`sonnet`), поисковик исследования (`sonnet`). Модель роли лежит в её определении и применяется сама — скилл только называет тип агента. **Проверено фактическим запуском 07.08.2026:** поисковик и исполнитель поднялись на Sonnet 5, блок-агент — на Opus 5, при том что сессия шла на Opus (то есть sonnet-роли доказывают, что работает определение, а не наследование). До 07.08.2026 модель была написана прозой в скилле, и на живом прогоне не передалась ни разу: все агенты молча шли моделью сессии. Разбор — [#20](https://github.com/GarroV/furca/issues/20). Модель **главной** сессии система задать не может в принципе (это `/model` владельца) — команды сверяют её с рекомендацией и говорят вслух, если расходится.
- **Форматы состояния** (`templates/state/`): задачи, вопросы владельцу, журнал событий, журнал решений. Это несущая конструкция: по ним упавшая сессия восстанавливает стройку, не перечитывая код.
- **Шаблоны документов проекта** (`templates/`): принципы, спека, техплан, описание блока, отчёт исследования.
- **Стартовая анкета** (`templates/intake.md`) — главный артефакт системы. Начинается с вопроса «что уже есть»: только идея, готовое ТЗ, работающий продукт с живыми пользователями, наработки кода, данные для переноса, готовый дизайн, чужой контракт, срок — и от ответа зависит, какие вопросы задаются дальше и какие правила включаются (например, живые пользователи запрещают проверки на живой базе). Дальше шесть секций от продукта до доступов, у каждого вопроса метка «нужен владелец» или «можно выяснить исследованием», в конце карта покрытия. Два заполненных примера на противоположных стартовых состояниях: `test/fixtures/toy-project/intake.md` и `test/fixtures/intake-brownfield.md`. На чём построена — [docs/research/intake-practices.md](research/intake-practices.md).
- **Протокол уведомлений** (`templates/telegram-protocol.md`): четыре типа сообщений — вопрос (пачкой, с рекомендацией), блок готов, алерт, MVP готов. Вопросы копятся и уходят пачкой, а не потоком; при выключенном канале вопрос всё равно попадает в файл и стройка не останавливается.
- **Сводка о ходе стройки** — первая работающая команда системы: показывает задачи по статусам, активные блоки, открытые вопросы, решения, принятые без владельца, последние события и следующий шаг.
- **Старт проекта** (`skills/ordo/SKILL.md`) — команда, которая доводит проект от одной фразы до пакета документов и останавливается до «ок» владельца. Умеет и новый проект, и работу внутри уже существующего репозитория; после каждой фазы коммитит, поэтому упавшая сессия продолжает с той же фазы, а не проходит анкету второй раз.
- **Автономная стройка** (`skills/fabrica/SKILL.md`) — диспетчер: раздаёт блоки агентам в отдельных копиях репозитория, принимает их с прогоном всех тестов и живым смоуком, задаёт владельцу только настоящие развилки и продолжает работать, не дожидаясь ответа, а в конце сверяет результат со спекой и запускает продукт. Одна и та же команда начинает стройку и продолжает её после падения сессии.
- **Раскатка на боевую площадку** (`skills/missio/SKILL.md`) — переносит согласованный MVP на целевую площадку из анкеты. Доступы перепроверяет заново, а не верит анкете; результат проверяет снаружи, по внешнему адресу; красный смоук называет неудачей и откатывает. Разворачивает только после явного «да» владельца в том же разговоре.
- **Разбор прогона** (`templates/retro.md`) — в конце стройки система разбирает, что мешало ей самой, и заводит задачу на каждый пункт в этом беклоге. Иначе следующий проект встретит те же грабли.
- **README** — описание системы для читателя: что это, установка, три команды обычными словами, что готово и что нет.
- **Разбор прогона** — по итогам обкатки система сама завела себе 4 задачи в беклог и **они закрыты**: шаблон брифа блок-агенту (`templates/block-agent-brief.md`), правила уборки за агентом, подсекция анкеты про адрес и порт запуска, штатный обход занятого браузера.
- **Канал уведомлений** (`channel/`) — бот и мини-сервер, **развёрнут на домашнем сервере 07.08.2026 и проверен с рабочей машины**: отправка доходит, ответ владельца забирается, пометка разобранного идемпотентна. Одна постоянная точка владеет ботом, поэтому уведомления больше не зависят от флага запуска сессии и не отбираются второй сессией. Как прошло развёртывание и на какое ложное «работает» не попасться — [channel/README.md](../channel/README.md). **С 26.08.2026 ответы владельца адресуются проекту, и это раскатано на живой канал в тот же день** (реплай → журнал отправленного → имя проекта): `GET /inbox?project=` отдаёт только своё, чужой `ack` получает `409` и ответ остаётся ждать адресата. До этого очередь была общей на все стройки, и вторая стройка съедала ответ первой молча — обе считали, что всё идёт по плану. Проверено не только тестами: на живом канале обновление прошло без потери данных, а ответ владельца реплаем получил адрес сам — то есть телеграмное плечо тоже пройдено, а не оставлено на веру.
- **Непрерывность стройки** (`hooks/keep-building.py`, с 22.08.2026) — сторож на событии `Stop`: пока в графе есть задача, которую можно взять, ход не отдаётся владельцу. До него автономность держалась на побочном эффекте харнесса (пробуждение сессии по завершении фонового агента) и рвалась в стыке между волнами: на живом прогоне диспетчер написал «беру следующую волну» и закончил ход. Поведение хука измерено, а не взято из документации: блокировать можно многократно, а `stop_hook_active` после первой блокировки залипает в `true` и счётчиком служить не может — поэтому защита от холостого цикла своя (три хода без изменения состояния — отпуск). Сторож молчит вне активной стройки, не мешает работающим агентам и не удерживает чужую сессию. Регистрируется установщиком; **в открытых сессиях начинает работать только после перезапуска Claude Code** — хуки снимаются снимком на старте.
- **Каталог проверок качества** (`skills/admissio/`, с 26.08.2026) — какие гейты выдавать построенному проекту и, что важнее, какие не выдавать. Шесть обязательных ролей (формат, линт с типами, проверка типов, тесты с порогом покрытия, мёртвый код, границы модулей) и ситуативные — у каждой условие включения **и** условие невключения, иначе опция становится дефолтом, который кто-то включит на всякий случай. Наборы по стекам: TypeScript, Python, Deno. Границы модулей выводятся из графа блоков техплана механически — у FURCA граф есть и утверждён на гейте, поэтому правило «блок импортирует только то, от чего зависит» не надо придумывать заново; это единственная проверка, которая ловит риск параллельной стройки, где блоки не видят друг друга до слияния. Версии инструментов сверены с реестрами пакетов 26.08.2026, **перепроверить 26.11.2026** или раньше по событию — основание, находки и способ перепроверки в [docs/research/2026-08-26-quality-gates.md](research/2026-08-26-quality-gates.md). Вшит в путь системы: `ordo` на этапе документов ставит конфиги и команды в репозиторий **до первой строки кода** и заполняет раздел «Проверки качества» техплана; приёмка блока прогоняет их рядом с тестами, и красная проверка означает то же, что красный тест; бриф блок-агенту и определение исполнителя требуют прогона до сдачи. Прогнан на субагентах 26–27.08.2026 шестью сценариями (по стекам, дисциплина ситуативных, монорепозиторий; два прогона из шести обрубил лимит сессии, но оба успели найти по ошибке). Нашлось пять ошибок в самих справках одного класса — команда попала в канон, не будучи запущена: выдуманный флаг порога у `deno coverage`, неработающая форма `root_packages`, типизированные наборы линта без `files`, правило запрета зависимостей без блока под `package.json`, устаревшая адресация порога покрытия в Vitest 4. Все исправлены и прогнаны на живых инструментах, в скилл добавлено правило «команда попадает в справку только после запуска». Что прогнано и что нет — в основании каталога.
- **Состав коммита и общая рабочая копия** (28.08.2026) — сквозное правило всех трёх ролей: коммит только перечислением путей, сверка `git diff --cached --name-only` до коммита, восстановление после проверки порчей копией файла, а не `git checkout`. Исполнителю коммит запрещён в самом определении роли, блок-агент после его возврата сверяет, что `HEAD` не двигался. Причина не гипотетическая: на живых прогонах сплошной `git add` четырежды забрал чужое — посторонний документ соседней сессии, каталог переводов в промежуточном состоянии, правку шаблона под сообщением про переводы и целую задачу исполнителя под сообщением про стили. Рядом закрыт след стройки в Docker: стенд блока гасится **до** снятия рабочей копии (после снятия гасить нечем — compose-файла уже нет), а осиротевшие стенды прошлых прогонов ищутся на старте сверкой `docker compose ls` с `git worktree list` — замерено, что два таких стека держали по ядру процессора и по своей базе четверо и пятеро суток, ничего при этом не сигналя. Правила закреплены контрактными проверками в `test/skills.test.sh`, `test/agents.test.sh`, `test/templates.test.sh` и проверены негативным прогоном на испорченных копиях. Разбор — [#27](https://github.com/GarroV/furca/issues/27), [#38](https://github.com/GarroV/furca/issues/38), [#41](https://github.com/GarroV/furca/issues/41), [#47](https://github.com/GarroV/furca/issues/47), [#54](https://github.com/GarroV/furca/issues/54), [#58](https://github.com/GarroV/furca/issues/58).
- **Приёмка проверяет прогон, а не код возврата** (28.08.2026) — тесты и статические проверки гоняются **из свежего клона** основной копии, а не в рабочей: красный клон при зелёной копии означает, что для прогона нужно то, чего в репозитории нет (`.env` без закоммиченного примера, собранные руками артефакты, заведённые данные). Раньше это лечилось правилом про `.env.example`, а правило проверялось глазами. Рядом закрыт класс «зелено на нуле выполненного»: приёмка читает машинный отчёт того прогона, который сделала сама, и сверяет три числа — выполнено, пропущено, заявлено в контракте блока; числа пишутся в журнал строкой приёмки, и следующая приёмка сравнивает с ними. Формат отчёта объявляется в техплане проекта (раннеры у проектов разные), ставится вместе с конфигами гейтов до первой строки кода и назван частью роли «тесты» в каталоге проверок. Сверка со спекой — самый результативный механизм по замеру (49% найденных дефектов против 0% у набора тестов блока) — обязана теперь проходить пути отказа и права: на них приходится 42% прорвавшегося мимо тестов. Разбор — [#48](https://github.com/GarroV/furca/issues/48), [#49](https://github.com/GarroV/furca/issues/49), [#50](https://github.com/GarroV/furca/issues/50).
- **Две развилки качества закрыты решением** (28.08.2026, владелец делегировал выбор). **Порог покрытия — относительный:** блок не принимается, если покрытие ниже, чем на прошлой приёмке этого проекта; абсолютная цифра (80% и подобные) осталась ориентиром в принципах проекта и сама по себе блок не заворачивает. Основание — замер 61 дефекта: абсолютный порог не поймал ни одного, зато подталкивает дописывать тесты без ассертов ради процента, а такой тест приёмка засчитывает выполненной проверкой. Относительный порог ловит то, ради чего роль заведена — молчаливую деградацию — и опирается на числа, которые приёмка теперь и так пишет в журнал. **Сверка со спекой стала двухуровневой:** частичная идёт на приёмке каждого блока (сценарии, которых блок касается, включая пути отказа и права), полная остаётся в конце и проверяет то, чего не видно поблочно — сквозные сценарии через несколько блоков, критерии готовности MVP, стыки. Дефект находится, пока контекст блока жив, а расход не растёт пропорционально числу блоков. Формулировка порога сверена между принципами проекта и Definition of Done блока проверкой: разойдутся — блок-агент и приёмка будут заворачивать по разным правилам. Разбор — [#48](https://github.com/GarroV/furca/issues/48), [#51](https://github.com/GarroV/furca/issues/51).
- **Стройка перестала вставать и обрываться молча** (28.08.2026) — шесть отказов одного рода: система продолжала работать, а признака поломки не подавала. **Граф против трекера:** сводка сверяет открытые задачи трекера с незакрытыми в графе и говорит вслух, если граф отстал; у задачи появилась колонка `issue`, поэтому сверка механическая, а не по тексту коммита; диспетчер перед отчётом «работа закончена» обязан отличить «всё сделано» от «граф протух». Повод — живой проект, где работа переехала в трекер, граф показывал 171 `done` при 74 открытых задачах, и стройка простояла полтора месяца, выглядя успешной. **Проверка целостности** получила три настоящие проверки вместо ссылочных: цикл в зависимостях (граф ссылочно целый, но задачи не станут доступными никогда), блок объявлен в пакете документов без единой задачи (цель недостижима), блок строится без описания в пакете (диспетчер его не увидит — на одном прогоне план знал девять блоков при двадцати трёх в журналах). **Сон машины:** фазы с фоновыми агентами держат машину бодрствующей на всю фазу, а не на агента, и владельцу называются границы защиты — крышка и батарея не покрываются; обрыв по сну не тратит попытку перезапуска, а оборвавшегося агента догоняют сообщением, а не запускают заново. **Сторож:** бриф предупреждает, что молчание дольше десяти минут срезает агента, и требует гонять по ходу только затронутое; цену полного прогона диспетчер замеряет сам и передаёт плейсхолдером. **Живые исполнители:** отчёт блока обязан их назвать, а приёмка — убедиться, что за блоком никого не осталось, прежде чем править его файлы; доделка диспетчером описана как штатный путь рядом с возвратом. **Причина в журнале — только проверенная:** при одновременной остановке агентов сначала проверяется сон машины, и «причина неизвестна» лучше правдоподобной версии, которая через день читается как факт. Разбор — [#22](https://github.com/GarroV/furca/issues/22), [#43](https://github.com/GarroV/furca/issues/43), [#53](https://github.com/GarroV/furca/issues/53), [#55](https://github.com/GarroV/furca/issues/55), [#70](https://github.com/GarroV/furca/issues/70).
- **Обновление ядра не ломает идущие проекты** (28.08.2026) — скиллы подключены симлинками, поэтому правка ядра применяется к уже идущим стройкам немедленно, при следующем вызове команды. Проверено на двух проектах, идущих на этой машине. Найдено и исправлено два дефекта. Первый жил в проверке целостности с момента её появления: запись о запуске блока засчитывалась, только если двоеточие стояло сразу после имени блока, а диспетчер штатно пишет в ней состав волны — проект с корректным журналом объявлялся сломанным и не мог строиться, причём сообщение уводило в другую сторону, утверждая отсутствие записи, которая есть. Второй — свежий: правила приёмки, введённые в тот же день, опирались на данные, которых у проектов, начатых раньше, нет (объявленный формат машинного отчёта, числа предыдущей приёмки). Теперь такое правило задаёт базу и заводит задачу на недостающее вместо того, чтобы останавливать стройку задним числом, — и говорит об этом в журнале, чтобы пропуск не был молчаливым. Разбор — [#73](https://github.com/GarroV/furca/issues/73), [#74](https://github.com/GarroV/furca/issues/74).
- **Канал перестал молчать о собственном отказе** (28.08.2026) — у него было два вида отказа, о которых он по построению не мог сообщить. Первый: процесс завис целиком, и `/healthz` не отдал ни `503`, ни `200` — ничего, потому что обслуживается тем же событийным циклом; контейнер при этом остался `Up`, и `restart: unless-stopped` не сработал. Теперь у сервиса есть внешний `healthcheck` (на Python, а не curl — в образе `python:3.12-slim` его нет, проверено запуском, и healthcheck на curl уводил бы исправный контейнер в `unhealthy`), а отметка последней удачной выборки лежит **в базе**: она переживает зависание и перезапуск процесса, поэтому по ней видно, когда именно канал замолчал, даже если спросить его самого уже нельзя. Второй: ответ владельца, который никто не забрал. Входящие берёт только стройка — пока она не идёт, ответ лежит в очереди неограниченно долго (измерено: три ответа, четыре и пять дней, один из них владелец считал принятым). `/healthz` теперь отдаёт число неразобранных и возраст самого старого, а сводка о ходе проекта их показывает — **показывает, но не помечает**: `ack` без применения ответа съедает его молча. Застой при этом намеренно не меняет статус: на `/healthz` завязан перезапуск контейнера, а неразобранный ответ перезапуском не лечится. Диспетчеру предписано считать неответ канала отказом канала, а не молчанием владельца — это разные состояния, и второе повод ждать, а первое чинить. Тесты канала: 30 → 42, все новые подтверждены негативным прогоном на испорченной копии. Разбор — [#52](https://github.com/GarroV/furca/issues/52), [#60](https://github.com/GarroV/furca/issues/60).
- **Задание агенту и анкета догнали то, что с них спрашивают** (28.08.2026) — пять разрывов между входом и выходом. `CHANGELOG.md` требовался приёмкой и не был назван в задании: два блока подряд вернулись на доработку, второй возврат дал конфликт слияния, который сводили руками. Теперь он назван там же, где журнал блока, с формулировкой «по смыслу и без id задач» — иначе агенты пишут в CHANGELOG номера задач, бесполезные читателю. Definition of Done блока получил пометку этапа: контракт пишется на блок целиком, а строится он очередями, и агент получал список, где часть пунктов относится к четвёртой очереди; приёмка спрашивает пункты своего этапа, а не всего блока. Анкета получила три вопроса, каждый куплен отдельным разбором: что уже живёт на целевой площадке (выясняется смоуком, а не спрашивается — на прогоне чужой стек на четырёх портах и чужие таблицы в общей базе всплыли после принятых решений), лежит ли источник данных внутри репозитория (решается до первого коммита, задним числом лечится вычищением истории) и работает ли кто-то в этой папке прямо сейчас (две работы в одном каталоге топчут друг друга через общий индекс git). Разбор — [#24](https://github.com/GarroV/furca/issues/24), [#25](https://github.com/GarroV/furca/issues/25), [#26](https://github.com/GarroV/furca/issues/26), [#28](https://github.com/GarroV/furca/issues/28), [#30](https://github.com/GarroV/furca/issues/30).
- **Документы проверяются машиной, а не только глазами** (28.08.2026) — три вида дрейфа, которые до сих пор ловились ручной сверкой или не ловились вовсе. **Формат решений** объявлен неизменным (его парсят стройка и сводка), но ничем не проверялся: проверка целостности теперь сверяет число колонок, дату и значение графы «кто». Сразу нашла пять битых записей в живом проекте — сдвиг колонок там показывал в сводке «кто = список задач» и решение, начинающееся со слова `owner`, и выглядел не ошибкой, а странностью. **Дата снимка** не может быть раньше событий, которые снимок описывает: читатель судит о возрасте документа по шапке и верит ей. Будущие даты из сравнения исключены — срок перепроверки не событие. **Входные документы** получили свой прогон (`test/docs.test.sh`): инвентарь шаблонов против дерева файлов, живость относительных ссылок, наличие у каждой команды фразы вызова из её же триггеров и запрет на инструкцию включать канал через плагин. Последнее — не гипотетика: такая инструкция стояла в README и ломала читателя молча, потому что выбитый слушатель не жалуется, а бот выглядит живым. Проверка фраз тут же нашла команду, которую нельзя было позвать: каталог проверок качества не упоминался в README вовсе. Все восемь новых проверок подтверждены негативным прогоном. Разбор — [#32](https://github.com/GarroV/furca/issues/32), [#46](https://github.com/GarroV/furca/issues/46), [#62](https://github.com/GarroV/furca/issues/62).
- **Проверка канала на старте спрашивает сам канал** (28.08.2026) — раньше стройка и раскатка проверяли «есть ли в этой сессии инструменты канала (сессия запущена без флага)». Флага не существует с тех пор, как канал стал постоянной службой: проверка описывала механизм, которого нет, и сработать правильно не могла. Теперь обе команды зовут `GET /healthz` и различают три исхода вместо двух: канал работает; канал жив, но **приём ответов отобран** вторым слушателем бота (`503 polling down`) — отправка при этом работает, поэтому владелец увидит вопрос и ответит, а ответ не дойдёт никогда; канал не ответил вовсе. Второй исход — ровно тот молчаливый сбой, ради которого задача заводилась: раньше он был неотличим от исправной работы. Разбор — [#18](https://github.com/GarroV/furca/issues/18).
- **Скан безопасности снова читаемый** (28.08.2026) — он краснел всегда и по одной известной причине (`ENTR-18`: канал слушает `0.0.0.0` внутри контейнера, иначе он не отвечает вовсе), а скан, который краснеет всегда, перестают читать — и следом в том же красном теряется первая настоящая находка. Находка принята в `.aitriage-baseline.json` с письменным обоснованием ([docs/security-baseline.md](security-baseline.md)): что подавлено, почему, и при каких событиях список пересматривается. Точечность подавления проверена запуском, а не предположена — временный файл с тем же `host="0.0.0.0"` в другом месте дал `1 new` и `AUDIT FAILED`. Устойчивость тоже: сдвиг строк в файле подавление не ломает, сопоставление идёт по хешу. Попутно найдена шероховатость инструмента — `baseline create` записывает абсолютный путь, то есть в общем репозитории и утечка личного пути, и неработающая запись на чужой машине; путь заменён на относительный (проверено, что подавление продолжает работать), проверка чистоты ядра расширена на сам файл baseline, а дефект заведён в трекер инструмента. Разбор — [#59](https://github.com/GarroV/furca/issues/59).
- **Экранный эталон — обязательный результат проектирования** (28.08.2026) — для любого продукта с интерфейсом, до стройки, а не после. Причина названа владельцем: по коду он не может судить, как продукт должен работать, а по картинке может, и решения на ней стоят минуты — после стройки те же решения стоят переделок. Замер с живого проекта: ревизия «эталон против продукта» нашла 63 расхождения по 17 модулям, две трети открытых задач в трекере были порождены самим разрывом, а у продукта оказалось три источника истины и ни одного главного. Теперь `ordo` собирает `docs/furca/design/` — статический HTML, который открывается двойным щелчком: карта экранов, экран на каждый ключевой сценарий, состояния (пусто, загрузка, ошибка, нет прав) и словарь компонентов, с настоящими текстами вместо «Lorem ipsum». Формат выбран так, чтобы владелец кликал, а не читал описание, и чтобы разметка потом переиспользовалась стройкой. Спека делит с эталоном ответственность: что на экране — эталон, как оно себя ведёт — спека; расхождение решает владелец записью в журнал. Блок `visual` стал обязательным в шаблоне плана, Definition of Done блока получил визуальные пункты, бриф требует сверки страницы с эталоном, приёмка — сверки экрана, а гейт проверяет, что эталон открывается, запуском, а не наличием папки. На случай, когда эталон приходит позже (так бывает), описан порядок слияния целиком: карта «модуль → блок → задачи», модули без блока называются вслух, спека догоняет эталон, DoD построенных блоков дополняется — и всё это до того, как стройка продолжится. Разбор — [#69](https://github.com/GarroV/furca/issues/69).
- **Приёмка проверяет сам гейт, а не только его цвет** (28.08.2026) — зелёный гейт означает «не нашёл» только если он смотрит туда, куда должен. На живом прогоне блок-агенту подложили каталоги данных симлинками на основную копию, и команда вида `cd examples/foo && python3 ../../engine/run.py` ушла по ссылке: агент правил движок у себя, а регресс два часа прогонял чужой, неизменённый — и честно зеленел, потому что считал правильно, просто не то. Теперь перед тем, как поверить зелёному, приёмка портит проверяемый код и убеждается, что гейт краснеет: то же правило «проверка, которая не падает на испорченном входе, — не проверка», применённое к гейту, а не к тесту. Оно ловит и другие случаи того же класса — собранный артефакт вместо исходника, интерпретатор не из той среды. Данные вне git теперь предписано отдавать блоку копией, а не симлинком; если объём не позволяет — агент предупреждается в брифе, что запускать из подложенных каталогов надо по абсолютному пути. Разбор — [#75](https://github.com/GarroV/furca/issues/75).
- **У построенного проекта остаётся гейт, а не только зелёная приёмка** (28.08.2026) — раньше приёмка гоняла проверки руками, и как только стройка кончалась, у проекта не оставалось ничего, что может покраснеть: замер по репозиториям владельца дал гейт, способный упасть, у пяти из шестнадцати, порог покрытия и хуки — ни у одного. Теперь `ordo` вместе с конфигами заводит **единую команду прогона** — исполняемый `scripts/check`, объявленный в техплане, — и хук `pre-push`, который зовёт её же. Хук кладётся в `.githooks/` и включается через `core.hooksPath`, то есть попадает в репозиторий и достаётся следующему человеку, а не остаётся на одной машине; и обязан падать, когда инструмента нет, а не пропускать проверку. Приёмка зовёт **объявленную** команду, а не собирает свою: собранная на месте расходится с тем, что гоняют хук и CI, и «зелено у меня» перестаёт что-либо значить. Команды нет — красное на первом же блоке. Разбор — [#51](https://github.com/GarroV/furca/issues/51).
- **В графе появились этапы, и достижимость цели проверяется машиной** (28.08.2026) — граф бывает ссылочно целым, ациклическим и при этом недостижимым: цель первой очереди упирается в задачу из четвёртой. Раньше это ловил человек, державший очереди в голове. У задачи появилась колонка `этап`, и проверка целостности запрещает зависеть от задачи более позднего этапа; тем же номером помечаются пункты Definition of Done в описании блока, чтобы агент видел свою часть контракта. Колонки нет — проверка молчит: правило введено позже графов, которые уже строились. Попутно из проверки целостности убран `xargs`: он интерпретирует кавычки и падал с «unterminated quote» на тексте задачи живого проекта, унося весь прогон, — а красная проверка целостности означает «стоп» для стройки. Разбор — [#23](https://github.com/GarroV/furca/issues/23).
- **Изоляция исполнителей стала механизмом, а не инструкцией** (28.08.2026) — изоляция в системе сделана на уровне блока: своя копия репозитория у блок-агента, а не у его исполнителей. Внутри блока их разводил только текст задания, и текст не сработал трижды: чужой файл уехал в чужой коммит, исполнитель закоммитил вопреки прямому запрету, работа второго исполнителя ушла под сообщение про правку стилей. Теперь у выдачи своей копии (`isolation: "worktree"`) есть три проверяемых условия: запускаются двое и больше сразу; файлы исполнителя пересекаются с теми, что блок-агент правит в это же время; исполнителю нужен прогон, меняющий состояние (сборка, миграции, стенд). Не «всегда изолировать» — на куске из одного файла слияние дороже самой работы. Определение роли исполнителя разведено по копиям: в общей — не коммитить, в своей — коммитить обязан, иначе работа никуда не переедет. Разбор — [#71](https://github.com/GarroV/furca/issues/71).
- **Пакет документов сверяется между собой машиной** (28.08.2026, `test/package.test.sh`) — спека, план, описания блоков и граф пишутся в одной сессии и расходятся молча: история `must` остаётся без блока и её просто никто не строит; блок описан, но план о нём не знает — диспетчер его не раздаст; контракт блока пуст — параллельные блоки не знают, чем обмениваются; секция не заполнена — агент строит по догадке. Ни один случай не падает, пакет выглядит целым. Чтобы связь спеки с планом стала машинной, у user story появилась колонка «блок». Проверка стоит гейтом дважды: `ordo` не отдаёт пакет владельцу без неё, а стройка не начинается по красному пакету — чинится это в документах, поэтому чинит диспетчер, а не блок-агент. Раньше требование «пустых шаблонных секций не осталось» стояло в списке гейта, но проверялось глазами того, кто эти секции и заполнял. Попутно заведена проверка на ловушку bash 3.2: переменная вплотную к кириллице или «кавычке» разбирается как имя вместе с первым байтом символа, и прогон умирает с «unbound variable» посреди работы — за сессию это случилось трижды в трёх файлах, и каждый раз ловилось только негативным прогоном. Разбор — [#16](https://github.com/GarroV/furca/issues/16).
- **Канал ставится мастером и по умолчанию локально** (28.08.2026, `channel/setup.sh`) — вопрос «почему не раздать бота посторонним» упёрся в то, что раздавать не нужно: канал носит внутренности чужих проектов, лимиты Telegram живут на токене, бан токена гасит уведомления всем сразу и молча, а мультиарендность — отдельный продукт с дежурством. Вместо раздачи убрана искусственная сложность своей установки. От человека остался один шаг — создать бота у @BotFather; мастер генерирует секрет, **сам определяет chat id** (просит написать боту и читает первое обновление, вместо стороннего бота и чтения логов), пишет `~/.claude/furca/channel.env` с правами 600, поднимает контейнеры и проверяет канал сквозняком — отправкой настоящего сообщения, а не «контейнер запущен». Существующие настройки без подтверждения не затирает, `--check` проверяет предусловия, ничего не меняя. Локальный режим сделан режимом по умолчанию: на своей машине отпадают публикация порта наружу, VPN, обход inbound-фильтра Docker Desktop и проверка «снаружи, а не с хоста» — Telegram опрашивается исходящими, стройка ходит по петле. Минус назван честно в обоих README: спящая машина уведомлений не получает, ответы полежат в Telegram около суток и подхватятся при пробуждении, стройка в это время тоже стоит. Разбор — [#35](https://github.com/GarroV/furca/issues/35).
- **Показ владельцу — механизм под класс дефектов, который не ловит ничто другое** (28.08.2026) — замер 61 дефекта выделил 13%, которые не берут ни тесты, ни сверка со спекой: непонятный онбординг, два ряда одинаковых кнопок, пропавшая подсказка доступности, разошедшиеся с продуктом локали. Тест проверяет заранее сформулированное утверждение, а это никто не формулирует — потому что оно видно и только видно; сверка проходит сценарий, и он проходится: путь работает, продукт при этом неудобен. У канала появился пятый вид сообщения — `show`: скриншоты уходят **фотографиями с подписью**, а не ссылкой на файл, потому что смысл в том, чтобы владелец увидел экран с телефона, ничего не открывая. Повод узкий: принят блок, изменивший видимое человеку — экран, текст для человека, порядок работы; показы копятся пачкой, как вопросы, иначе они превращаются в поток, который перестают открывать. Показ **не блокирует стройку**: ушёл и пошли дальше, ответ подхватится на следующей приёмке. Снимки берёт блок-агент там, где и так открывал браузер. Тесты канала: 42 → 47 по базе и 43 → 50 по логике, включая пределы (до 10 картинок, до 5 МБ) и отдельную отправку длинного текста, который не влезает в подпись. Разбор — [#72](https://github.com/GarroV/furca/issues/72).
- **Внешняя сверка по тестам: что берём из индустрии, а что нет** (28.08.2026, [docs/research/2026-08-28-testing-industry.md](research/2026-08-28-testing-industry.md)) — свой замер дал неудобный результат (набор тестов блока поймал 0% из 61 дефекта), и принимать его по одной выборке нельзя. Сверка нашла подтверждение и одну находку, бьющую прямо в нас: у наборов, **написанных LLM**, покрытие ещё о чём-то говорит на заведомо корректном коде, но когда код-под-тестом может содержать дефект — а агент пишет тесты к тому, что сам сейчас пишет, — зависимость рассыпается полностью. То есть покрытие слабее всего работает именно там, где мы его применяем; решение держать порог относительным получило основание, которого у него не было. Мутационный прогон как гейт отвергнут: корреляции с находимостью настоящих дефектов слабые при контроле размера набора, а счёт зависит от выбора операторов — но наш ручной вариант («проверка, которая не падает на испорченном входе, — не проверка») остаётся, он адресный и без иллюзии метрики. **Взято два.** У каждого контракта между блоками теперь исполняемая проверка с обеих сторон — блоки идут параллельно и не видят друг друга, поэтому расхождение всплывало при слиянии у того, кто его не вносил; это ровно случай, ради которого придумано контрактное тестирование, но без брокера — стороны живут в одном репозитории. И раскатка обязана оставить **повторяемый** смоук плюс записанный способ отката: по нашему же аудиту смоука после раскатки не было ни у одного из 16 репозиториев, а устойчивость определяется скоростью восстановления, а не отсутствием отказов. Разбор — [#44](https://github.com/GarroV/furca/issues/44).
- **Правило, которого проект не мог знать, не останавливает работу** (28.08.2026) — общий принцип, выведенный после того, как за один день в стройку добавилось четыре новых требования, и все четыре молча оказались блокирующими для проектов, начатых раньше. Проверка на двух живых проектах показала это фактически: у обоих нет объявленной команды прогона и экранного эталона, у одного красный пакет документов — то есть ни один блок не был бы принят, а одна стройка не началась бы вовсе. Принцип: **нет артефакта вовсе — задача `chores` и запись в журнал, что проверка не делалась; артефакт есть, но красный — стоп.** Разница существенная: отсутствие означает, что требование новее проекта, красный артефакт — что дефект кто-то внёс. Отдельно смягчена проверка пакета на старте: красное больше не останавливает стройку целиком, а тормозит только тот блок, которого расхождение касается — расхождение в описании соседнего блока остальным не мешает.
- **Английский канон: первый этап — роли и задание блок-агенту** (29.08.2026) — система передаётся другим людям, и канон у неё английский: команды, определения ролей и шаблоны читает не только владелец. Переведены три определения ролей и `templates/block-agent-brief.md` — самый нагруженный шаблон системы, 260 строк правил, каждое из которых куплено живым прогоном. Перевод не буквальный: сохранены не формулировки, а факты и цифры, ради которых правила существуют, — иначе текст теряет то единственное, что заставляет им следовать. Вместе с ними переведены русские литералы в контрактных проверках (`test/agents.test.sh`, `test/templates.test.sh`): проверки опознают правила по фразам, и без синхронного перевода они начали бы падать на верном тексте. Переведённые правила подтверждены негативными прогонами — снятие любого из них роняет проверку. Остальные этапы (шаблоны, команды, анкета, README) — в [#11](https://github.com/GarroV/furca/issues/11).
- **Тесты** (14 наборов, сверено с `test/` 17.09.2026): `test/package.test.sh` (согласованность пакета документов проекта), `test/docs.test.sh` (входные документы: дата снимка, инвентарь шаблонов, живость ссылок, фразы вызова команд, запрещённые инструкции), `test/install.test.sh`, `test/state-format.test.sh`, `test/templates.test.sh`, `test/skills.test.sh`, `test/agents.test.sh`, `test/channel.test.sh`, `test/channel-db.test.sh`, `test/hook.test.sh`, `test/commit-scope.test.sh` и `test/wave-width.test.sh` (два стража стройки), `test/compact-prep.test.sh` и `test/image-reads.test.sh` (подготовка к компакту и запрет картинок). С 17.09.2026 зелёные **все 14**: окружение для тестов базы канала поднимается одной командой `bash test/setup-channel-venv.sh`, и прогон находит его сам ([#99](https://github.com/GarroV/furca/issues/99)). Тестам базы канала нужен работающий Postgres и `asyncpg`/`aiohttp`/`aiogram`; нет их — прогон **падает** и говорит, чего не хватает, а не пропускает проверки молча. Прогоняются просто: `bash test/<имя>.test.sh`. Тесты изолированы (не пишут в реальный домашний каталог) и проверены на испорченных копиях — каждая проверка реально падает, когда должна.

- **Два правила стройки переведены в механизмы** (31.08.2026) — состав коммита и ширина волны. Оба правила были записаны прозой, подробно, с купленными уроками, и оба нарушались: сплошной `git add` дважды за три дня унёс чужие файлы в коммиты стройки ([#78](https://github.com/GarroV/furca/issues/78)), а волна из четырёх блок-агентов сожгла окно лимита вчетверо быстрее и оборвалась целиком, потеряв у всех четверых именно проверку ([#76](https://github.com/GarroV/furca/issues/76)). Теперь их держат хуки: `guard-commit-scope.py` не пропускает `git add -A`/`git add .`/`git commit -a` во время стройки и показывает состав индекса перед коммитом, `guard-wave-width.py` считает допустимую ширину волны от остатка лимита — молчит до 70% выбранного окна, дальше предупреждает, на исходе оставляет одного агента. Оба ставятся установщиком, снимаются `--off` вместе со сторожем и видны в `--check`, который заодно печатает остаток лимита для журнала. 34 новых теста, включая проверку мутацией: сломанная реализация роняет их.

- **Картинки ушли из контекста opus, компакт перестал заставать врасплох** (06.09.2026, [#95](https://github.com/GarroV/furca/issues/95)) — сверка построенного экрана с эталоном обязательна: она ловит 13% дефектов, невидимых ни тестам, ни сверке со спекой. Но снимок весит 30–180 тысяч токенов, остаётся в контексте до конца работы агента и перечитывается на каждом его следующем ходу; замер одной стройки — 40 снимков стоили 132 млн токенов чтения кэша, 20% всего расхода блок-агентов, целиком на opus. Неверным было не действие, а место: сверку делает роль `norma` на sonnet (живёт десятки ходов вместо сотен, возвращает расхождения текстом), а чтение картинки любой другой ролью во время стройки запрещает `hooks/guard-image-reads.py` — и его отказ **называет замену**, иначе агент решит, что сверка недоступна, и тихо её пропустит. Рядом закрыт второй расход: компакт, пришедший между «результат получен» и «результат записан». Сторож непрерывности читает размер контекста из транскрипта и за десятую долю до заданного владельцем порога автокомпакта удерживает ход ровно один раз — ради записи статусов, журнала и решений; порог не задан — сторож молчит. **16.09.2026 у стража нашёлся отказ собственного класса** ([#111](https://github.com/GarroV/furca/issues/111)): он не узнавал саму `norma` и заворачивал её же, а текст отказа вёл роль по кругу — починено с негативным прогоном.
- **Имя продукта подбирается отдельной фазой** (11.09.2026, `skills/ordo/product-name.md`) — раньше имя всплывало по ходу анкеты и им становилось рабочее слово из первой фразы владельца, которое потом расходилось с продуктом и оставалось в репозитории, путях и текстах. Теперь это отдельный шаг старта со своим выходом и своим гейтом владельца.
- **Окно подписки делится между стройками, состояние коммитится по ходу** (15.09.2026, [#105](https://github.com/GarroV/furca/issues/105), [#106](https://github.com/GarroV/furca/issues/106)) — два отказа одного дня. Первый: окно подписки одно на аккаунт, а стройки идут в разных проектах и друг о друге не знают — 15.09 три стройки стартовали в одно окно, каждая считала остаток своим, и втроём они выбрали его за пять часов, не дойдя ни одна до раздачи блоков. Ограничитель ширины волны (`hooks/guard-wave-width.py`) делит остаток на число живых строек по реестру маркеров в `~/.claude/furca/builds/`. Второй: решения владельца, статусы и журнал живут в рабочем дереве в единственном экземпляре, пока не закоммичены, а единственная точка сохранения стояла на пороге лимита — то есть там, где у сессии меньше всего шансов её исполнить (порог срабатывает на 99–100%). Теперь сторож непрерывности требует коммит после двух ходов расхождения файлов состояния с `HEAD`, один раз за эпизод, а порог **останова стройки** отвязан от порога **сохранения**: остановить стройку значит выбросить весь остаток окна, сохраниться — это один коммит, и одна цифра на два разных решения не годится. Он же перестал верить снимку лимита старше 15 минут: 15.09 стройка встала по цифре полуторачасовой давности, хотя окно успело сброситься и имело 4,5 часа запаса.
- **Холостое ожидание запрещено правилом, а не догадкой** (16.09.2026, [#114](https://github.com/GarroV/furca/issues/114)) — `sleep 300; <опрос>` в Bash не ждёт: харнесс уводит команду в фон и возвращает управление через секунду, агент получает пустой ответ и делает следующий ход, а каждый ход стоит полного перечитывания контекста. Замер одного захода: 24 попытки ждать, 165 холостых ходов из 398, 50 млн токенов из кэша, **29% расхода захода за 15 минут простоя** — при внешне нормальном отчёте, потому что ничего не падает. Правило добавлено туда, где его читают: бриф блок-агента, определение роли `optio` и скилл диспетчера — ждать внешнее событие только `Monitor` с until-циклом, два хода подряд без правок файлов считать опросом, а не работой. Диспетчеру отдельно сказано не опрашивать запущенную волну: сессия просыпается сама по завершении фонового агента, а его собственный опрос стоил 19,6 млн токенов из кэша за тот же отрезок.

- **Три молчаливых дефекта стройки закрыты разом** (17.09.2026, [#77](https://github.com/GarroV/furca/issues/77), [#115](https://github.com/GarroV/furca/issues/115), [#91](https://github.com/GarroV/furca/issues/91), [#109](https://github.com/GarroV/furca/issues/109), [#99](https://github.com/GarroV/furca/issues/99)). **Время в журнале берётся у системы:** его подставляла модель по своему представлению, и журнал обгонял часы — на одном заходе на 25 минут, на другом на 2 ч 40 мин, уводя записи в следующие сутки; сверка с `git log` давала противоречие «событие записано, коммитов после него нет». Теперь скилл требует `date '+%Y-%m-%d %H:%M'` в момент записи, формат журнала называет источник времени, а проверка формата состояния заворачивает записи из будущего — подтверждено негативным прогоном. **Каталог-черновик выдаётся на блок:** копии репозитория, порты и имена compose у блоков свои, а черновик был один на сессию — и бриф при этом велит уводить длинный вывод в файл, имя которого у всех одинаковое. На волне 16.09 два блока писали прогон в один `check.log`: второй `>` усёк первый, `EXIT=` появился дважды и стал неотличимым, один блок десять минут читал чужой прогресс как свой; раньше тем же способом сосед перезаписал блоку его оснастку. Теперь диспетчер создаёт `<черновик сессии>/<блок>/` и называет его в брифе рядом с портами, а контрактная проверка следит, чтобы плейсхолдер из брифа не пропал. **Тесты базы канала перестали быть вечно красными:** `bash test/setup-channel-venv.sh` ставит закреплённые версии из `channel/bot/requirements.txt` в `~/.claude/furca/channel-venv` на python3.12 (тот же, что в боевом образе канала), а прогон находит venv сам — 47 проверок адресации ответов, месяцами не гонявшихся на этой машине, прошли.

- **Маркер стройки достаётся диспетчеру, а копия блока — оснащённой** (17.09.2026, [#108](https://github.com/GarroV/furca/issues/108), [#113](https://github.com/GarroV/furca/issues/113)). **Кто владеет стройкой.** Сторож непрерывности назначал владельцем маркера «первую сессию, которая до него дошла», — а это как раз не диспетчер: он занят и ходов не заканчивает, до события `Stop` первой доходит любая соседняя сессия в том же проекте. 15.09.2026 маркер перехватила сессия ремонта: диспетчер на следующем ходу получил «маркер принадлежит другой сессии» и потерял непрерывный режим, посторонняя сессия три хода подряд получала приказ строить чужой проект, а требование закоммитить состояние ушло к ней же — на файлы, которые ей трогать нельзя. Теперь владелец записывается в момент `--start` из `CLAUDE_CODE_SESSION_ID` — команду стройки зовёт сам диспетчер, и гадать не нужно; ничей маркер (стройка начата старой версией) присваивает только сессия, которая уже сдвинула `tasks.md` или `progress.md`. Обе гарантии подтверждены негативным прогоном. **Чем гонять в копии блока.** `git worktree add` даёт только то, что лежит в git: окружения прогона в копии нет, а объявленные команды гейтов зовут интерпретатор по относительному пути внутри неё. 16.09.2026 два блок-агента независимо отчитались, что не вызвали ни одной объявленной команды и гоняли проверки системным интерпретатором — то есть не тем, чем гоняют приёмка и CI, и зелёное у них ничего не значило; второй по той же причине отказался селить исполнителей в отдельные копии, и предписанная изоляция оказалась неисполнимой. Диспетчер теперь поднимает окружение канонической командой проекта и **проверяет его запуском** объявленной команды гейтов, а бриф запрещает агенту тихо откатываться на команду, собранную на месте.

## Обкатка на служебном проекте (30.07.2026)

> Продукт был выбрасываемым и удалён вместе со своей базой. Журнал стройки и разбор прогона сохранены: [docs/runs/2026-07-30-smoke-urlshort/](runs/2026-07-30-smoke-urlshort/).

Система прогнана на выбрасываемом проекте «сокращалка ссылок» — от одной фразы до работающего продукта, без владельца. Результат: **продукт работает** (99→120 тестов, покрытие 99%+, сквозной сценарий проверен в живом браузере настоящими кликами), и **найдено больше десяти дефектов самой системы**, каждый — запуском, а не чтением кода.

Что подтвердилось работающим:
- **работа не теряется при падении агента** — блок-агент умер посреди задачи, уцелели коммиты и журнал в файле блока; правила «коммить мелко» и «журнал по ходу» отработали на реальном падении;
- **приёмка имеет зубы** — блок был **не принят** и возвращён агенту: он решил вопрос, вынесенный владельцу, поломав при этом стандарт HTTP;
- **сверка со спекой свежим взглядом находит недострой** — нашла недоделанную must-историю, которую все предыдущие проверки пропустили, потому что формальный список готовности её не требовал;
- **разделение владения файлами** — два блок-агента ни разу не залезли в общие файлы состояния и в чужой блок.

Главные дефекты, исправленные по ходу: смоук доступа, который проходит всегда (`dig` на несуществующем домене); отчёт research, который выглядит полным без трети данных; потеря всей работы агента из-за длинного финального ответа; конфликт слияния на каждой приёмке из-за общих файлов состояния; «сервер остановлен», который ложь, потому что проверялся код команды, а не порт; тесты, зелёные у агента и красные после слияния, потому что `.env` не переезжает.

## Что осталось до собранной v1

Всё, что можно написать и проверить без владельца, — сделано. Незакрытых задач четыре, и все четыре ждут человека: анкета на живом человеке ([#3](https://github.com/GarroV/furca/issues/3)), токен бота ([#2](https://github.com/GarroV/furca/issues/2)), раскатка на настоящую площадку ([#5](https://github.com/GarroV/furca/issues/5)) и пилот ([#7](https://github.com/GarroV/furca/issues/7)). По плану **v1 считается собранной после пилота** — он и есть приёмка системы. Работа давно идёт в `main` короткими ветками под задачу, которые вливаются сразу после зелёного прогона; отдельной ветки версии больше нет.

## Что нужно от владельца

1. **Полчаса у экрана на первый прогон** — задача 7 ([#3](https://github.com/GarroV/furca/issues/3)): система запускает старт проекта на игрушечном примере, владелец отвечает на анкету коротко (или «решай сам» почти везде). Это единственный способ проверить, что вопросы понятны живому человеку. Можно совместить с пилотом (задача 11), если сразу возьмём настоящий мини-проект.
2. **Ничего.** Канал развёрнут на домашнем сервере и проверен в обе стороны; плагин Telegram в Claude Code выключен, потому что выбивал канал. Осталось только дождаться первой настоящей стройки, которая пришлёт «часть готова» через канал (задача 5, [#2](https://github.com/GarroV/furca/issues/2)).
3. **Выбрать реальный мини-проект для пилота** (задача 11, [#7](https://github.com/GarroV/furca/issues/7)) — что-то нужное, но маленькое, на 1–3 блока.

## Как продолжить

Работа возобновляется с задачи, следующей за последней закрытой; порядок и зависимости — в конце файла плана. Достаточно сказать в новой сессии: «продолжаем FURCA» — состояние читается из этого файла, плана, Issues и истории коммитов.

Внутренний журнал исполнения (кто что делал, находки ревью, принятые решения по спорным местам) лежит в служебном каталоге `.superpowers/sdd/2026-07-29-forge-v1/` — он намеренно не в репозитории, но переживает перезапуск сессии.

## Решения, принятые по ходу

- **Публичные артефакты пишутся как рабочие документы, а не как переписка с владельцем** (решение владельца 26.08.2026, [#45](https://github.com/GarroV/furca/issues/45)). Формулировка владельца: проект должен выглядеть как взрослая профессиональная разработка — то, что его строит агент, тона не оправдывает. Отсюда три следствия. Первое: тон задан один раз и механически, в `templates/issue-style.md`, потому что задачи и комментарии пишет система, и без стандарта каждый агент выбирает стиль сам. Второе: обращение к владельцу на «ты» и оценки собственной работы из доков и трекера убраны — репозиторий читают снаружи. Третье: имена личной и рабочей инфраструктуры, состав персональных данных и дословные цитаты из переписки в публичные артефакты не попадают вообще, независимо от видимости репозитория: видимость меняется, а история правок остаётся. Проверка чистоты расширена с ядра на `docs/` и `README.md` ([#61](https://github.com/GarroV/furca/issues/61)).

- **Систему делаем пригодной к передаче другим людям** (решение владельца 13.08.2026). Это постоянный контекст разработки, а не отдельная задача: каждая правка проверяется вопросом «сможет ли ей воспользоваться посторонний». Отсюда — **канон системы английский**, а язык разговора с владельцем и документов его проекта берётся из профиля (`language`, дефолт `en`). Двуязычных файлов не делаем: два текста об одном факте расходятся, и протухает как раз тот, который читает новый человек. Задачи про переносимость перестают быть «вне скоупа» — это блокеры передачи.

- **Роль исполнителя оставлена, но переписана из разрешения в предписание** (08.08.2026, [#36](https://github.com/GarroV/furca/issues/36)). Разбор транскриптов живого прогона: за 26 запусков блок-агентов `optio` не поднялся **ни разу** — делегирование было написано как «дробить можно», а разрешением никто не пользуется: контекст уже под рукой, а лишний шаг стоит времени. Вся механика блоков шла на `opus`, ради экономии на которой роль и заводилась. Удалять роль не стали: работа для неё настоящая, не было механики. Теперь в брифе (агент читает именно его, а не скилл) стоят четыре наблюдаемых условия, при которых задача **обязана** уйти исполнителю, и требование отметить по каждой задаче — исполнителем она сделана или самим агентом; приёмка эту отметку проверяет и блок без неё не принимает. Тот же класс дефекта, что [#20](https://github.com/GarroV/furca/issues/20): роль, описанная прозой, не применяется — и это незаметно, пока не появится след. **Проверено запуском 08.08.2026:** блок-агент по новому брифу отдал исполнителю механическую задачу с готовым контрактом и **не отдал** вторую, сославшись на условие про аутентификацию; обе отметки в журнале блока на месте.
- **Специализированный разбор добавлен в приёмку — по факту, а не по здравому смыслу** (08.08.2026). Премису («ревьюер по базам поймал бы дефекты прав раньше сверки») проверили на настоящем дифе живого прогона: ревьюер по одному коммиту нашёл, что разрешающие политики PostgreSQL объединяются через ИЛИ и потому «сужающая» политика не сужает ничего, что участник может поднять себе права правкой собственной роли, что утверждённое не защищено от изменения и что скрытое течёт через итоги. Ту же стройку эти дефекты прошли насквозь и всплыли позже. Триггер шага сделан механическим — по именам файлов и содержимому дифа, а не по впечатлению; имена агентов-ревьюеров не зашиты, потому что это агенты владельца, а у другого их может не быть, и тогда диспетчер ведёт разбор сам по тому же перечню.
- **Каждый запуск агента пишется в журнал с ролью и моделью** (08.08.2026, [#29](https://github.com/GarroV/furca/issues/29)). То, что роль исполнителя не работала две стройки подряд, вскрылось только ручным разбором транскриптов. Проверка формата состояния теперь требует такую запись для каждого блока, который уже работал.

- **Ядро отделено от личного.** Ни в шаблонах, ни в скиллах, ни в фикстурах не должно быть имён серверов, хостов и ников владельца — только в личном профиле. Ревью дважды поймало нарушение этого правила, оба исправлены.
- **Противоречие в плане** («фикстура демонстрирует все статусы» против точных количеств строк, которые нужны проверке задачи 6) закрыто второй мини-фикстурой на пограничные статусы. Оба требования выполняются.
- **Утверждение «проверено» принимается только с приложенным выводом команды.** Введено после случая, когда исполнитель описал результат проверки, которую не выполнял, — и результат оказался неверным.
- **Проверка целостности зависимостей** между задачами добавлена сверх плана: висячая ссылка на несуществующую задачу заставила бы диспетчера ждать вечно, а это молчаливый сбой.
- **Готовых образцов анкеты в открытом доступе нет** — такие опросники живут в Notion агентств, а не в git. Поэтому анкета написана своя, а из чужого взяты только механики (карта покрытия и протокол уточняющих вопросов — из spec-kit, лицензия MIT).
- **Беклог заведён на GitHub** (11 issues): до этого он существовал только внутри файла плана, то есть был недоступен с телефона и из чужой сессии.
- **Анкета не предполагает проект с нуля.** Первая версия молча считала, что продукта ещё нет и владелец знает, чего хочет: продукт с живыми пользователями получал те же вопросы, что идея в одну фразу. Добавлена секция «что уже есть» с правилами ветвления от неё — по требованию владельца покрыть все стартовые состояния. Решения по трём оставленным владельцу развилкам приняты по делегированию и записаны в research-отчёт.
- **Проверка «пример не отстал от шаблона»** добавлена сверх плана: на заполненный пример анкеты будет ориентироваться `ordo`, и переименованная в шаблоне секция превратила бы образец в неверный молча.
